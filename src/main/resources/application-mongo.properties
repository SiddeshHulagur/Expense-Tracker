# Mongo profile configuration overrides
# Activate with: SPRING_PROFILES_ACTIVE=mongo

# Connection string to your MongoDB instance. Prefer setting MONGODB_URI env var so secrets stay out of source control.
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://Siddesh:<password>@cluster0.sz381cf.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0}

# Optional: customize the database name if it isn't part of the URI
# spring.data.mongodb.database=

# Reuse the same port fallback as the default profile
server.port=${PORT:8081}
